import React from "react";
import MainContainerDiv from "./style";

const MainContainer = ({ children }) => {
  return <MainContainerDiv>{children}</MainContainerDiv>;
};

export default MainContainer;
